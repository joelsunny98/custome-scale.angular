import { MajorTicksArrayPipe } from './major-ticks-array.pipe';

describe('MajorTicksArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new MajorTicksArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
