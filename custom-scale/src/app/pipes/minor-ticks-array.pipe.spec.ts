import { MinorTicksArrayPipe } from './minor-ticks-array.pipe';

describe('MinorTicksArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new MinorTicksArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
